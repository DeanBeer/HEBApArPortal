require "spec_helper"

describe Nrb::HebApArPortal do
  it "has a version number" do
    expect(Nrb::HebApArPortal::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
